#!/usr/bin/env bash
# Optional local hook — enable once per clone:
#   git config core.hooksPath .githooks
set -euo pipefail
ROOT="$(cd "$(dirname "$0")/.." && pwd)"
exec bash "$ROOT/tests/run-gates.sh"
