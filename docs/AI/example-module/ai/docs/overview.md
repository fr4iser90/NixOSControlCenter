# Domain: example (template)

Replace this stub when implementing a real module.

- Put one JSON file per tool under `ai/tools/`.
- Wrap existing `ncc <domain> …` verbs only.
- Document permissions and rebuild expectations here.
