# SSH Manager — Client

Outbound SSH connection manager (`ncc ssh client`): hosts, keys, and connection helpers. Enable with `client.enable` on the parent ssh-manager module.

## Documentation

- CLI contract: [CLI.md](./CLI.md)
- Parent module: [../README.md](../README.md)
