# server/default.nix
# Base module for server packages
# This module is loaded when any server sub-module is active

{ config, lib, pkgs, ... }:
{
  # Base server configuration can be added here if needed
}
