{
  # Module Manager Configuration
  # Module-manager is always active (Core module, no enable option)
  # Individual module configurations are managed via their own config files
  # in /etc/nixos/configs/core/*/config.nix and /etc/nixos/configs/modules/*/config.nix
}
