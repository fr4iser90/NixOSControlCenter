{
  # Core module - always active (no enable option)
  # NixOS Control Center is the main system management framework
  # Host policy: skip cli-registry dangerous yes/no (prefer per-run -y when possible)
  dangerousIgnore = false;
}
