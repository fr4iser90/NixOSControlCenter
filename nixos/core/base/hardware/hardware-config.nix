{
  system = {
    hardware = {
      enable = true;
      cpu = "intel";
      gpu = "amd";
      ram = {
        sizeGB = 8;  # Default 8GB, will be corrected by system-checks
      };
    };
  };
}

