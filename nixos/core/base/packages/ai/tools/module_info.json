{
  "name": "domain.packages.module_info",
  "description": "Show details for a package set or preset (what it contains).",
  "risk": "read",
  "permission": "package.user.self",
  "confirm": false,
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Set or preset name (e.g. gaming, gaming-desktop, user-web-tools)"
      }
    },
    "required": ["name"]
  },
  "argv": ["ncc", "packages", "module", "info", "{{name}}"]
}
