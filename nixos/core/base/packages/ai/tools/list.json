{
  "name": "domain.packages.list",
  "description": "List configured single packages (JSON). Default: current user. system=true → systemPackages (needs privilege).",
  "risk": "read",
  "permission": "package.user.self",
  "confirm": false,
  "inputSchema": {
    "type": "object",
    "properties": {
      "system": {
        "type": "boolean",
        "default": false,
        "description": "List systemPackages instead of current userPackages"
      }
    }
  },
  "argv": ["ncc", "packages", "list", "--json", "{{flag:system}}"]
}
