{
  "name": "domain.packages.module_add",
  "description": "Enable a package set or preset (system sets → packageModules; user-* presets → userPackages). Config only (--no-build).",
  "risk": "write",
  "permission": "package.user.self",
  "confirm": true,
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Set or preset name to enable"
      }
    },
    "required": ["name"]
  },
  "argv": ["ncc", "packages", "module", "add", "{{name}}", "--no-build"]
}
