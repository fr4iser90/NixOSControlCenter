{
  "name": "domain.user.delete",
  "description": "Delete a user account from systemConfig (no rebuild). Requires admin or restricted-admin. Cannot remove the last privileged account.",
  "risk": "write",
  "permission": "user.delete",
  "confirm": true,
  "inputSchema": {
    "type": "object",
    "properties": {
      "name": {
        "type": "string",
        "description": "Username to delete"
      }
    },
    "required": ["name"]
  },
  "argv": ["ncc", "user", "delete", "{{name}}"]
}
